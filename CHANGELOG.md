# 更新日志

本文件汇总 Video Download King 的正式版本变更。日期均为项目本地提交日期。

## 0.8.5 - 2026-08-29

### 新增

- 抖音自研引擎在分析后列出真实视频版本，显示分辨率、码率、编码和水印状态，并按用户选择精确下载资源。
- 小红书视频笔记新增真实版本选择，显示分辨率、码率、编码和体积，继续优先保留无水印原始视频。
- B站将画质和编码合并为真实版本列表，展示清晰度、分辨率、帧率、AVC/HEVC/AV1、动态范围和码率，音质仍可独立选择。
- 单链接、抖音、B站和小红书页面共用唯一下载根目录，任意页修改后立即同步并持久化。
- 各平台分析后显示本地 Cookie 状态，检查文件是否存在、平台域名、是否过期和常见登录凭据，区分未配置、失效、游客和可能有效的登录状态。

### 改进

- 单链接、抖音和小红书封面预览增加平台 Referer、User-Agent、重定向、代理和候选图回退，减少图片空白或被 CDN 拒绝。
- 抖音版本下拉框、小红书版本下拉框和 B站版本下拉框会占用可用水平空间，长版本说明不再被挤成看不清的短文本。
- 随包 `yt-dlp` 更新至 `2026.08.19`。

### 文档

- README 增加 Chrome 扩展“Get cookies.txt LOCALLY”的准确 Chrome 应用商店链接、Netscape `cookies.txt` 导出步骤和登录凭据安全提示。

### 验证

- 142 项自动化测试、编译检查和 Git 差异检查均通过。
- 完成 ZIP 内容、便携运行时、Cookie/本地配置排除检查和打包 EXE 启动冒烟测试。
- 发布包：`VideoDownloadKing-v0.8.5-Windows-x64.zip`
- ZIP 体积：`192.66 MiB`
- SHA256：`C266B1588F100A05BA78E9F0EBCE75FF8243BB0CFB4879D5F1EEA60D47FB944B`

## 0.8.4 - 2026-08-28

### 修复

- 小红书无水印原视频下载完成后直接原样保存，不再自动探测、重封装或转码。
- 原视频为 HEVC/H.265 时保持其原始编码、码率、音频和 MP4 封装参数，不根据本机播放器能力改变文件。

### 验证

- 137 项隔离基线自动化测试、编译检查和 Git 差异检查均通过。
- 使用真实小红书分享链接完成无水印 `originVideoKey` 下载，并核对 CDN 原文件与程序最终文件的 SHA256 完全一致。
- 发布包：`VideoDownloadKing-v0.8.4-Windows-x64.zip`
- ZIP 体积：`175.83 MiB`
- SHA256：`B7BEEA53D4BE134F6BA19D06C1D10C08048F7127FF0E3E4F1B9FA0EF8D6D6D05`

## 0.8.3 - 2026-08-28

### 修复

- 小红书分析改用仍提供 `originVideoKey` 的移动端笔记数据，优先下载无水印原视频，不再把桌面网页的 EF4 带水印播放流当作最终资源。
- 无水印原视频为 HEVC/H.265 时，使用便携包内 FFmpeg 自动转为 H.264/AAC MP4；转换通过 FFprobe 验证后原子替换，兼顾无水印与 Windows 播放兼容性。
- 下载与兼容性转换纳入同一单调进度，取消任务时会同时终止 FFmpeg 子进程。

### 验证

- 138 项隔离基线自动化测试、编译检查和 Git 差异检查均通过。
- 使用真实小红书分享链接完成匿名分析、无水印原视频下载及兼容性转换；最终输出为 720×1280、H.264 + AAC MP4，18.2 秒全片解码无错误。
- 发布包：`VideoDownloadKing-v0.8.3-Windows-x64.zip`
- ZIP 体积：`175.83 MiB`
- SHA256：`25138F96410FE4222532CA11D902470F2D152801E2902514BC446DA14A688F63`

## 0.8.2 - 2026-08-28

### 修复

- 小红书选流会跳过 FFmpeg 和常规播放器无法识别的 EF5/EF6/EF7 私有编码，避免下载成功但视频无法播放。
- EF4 流按标准 H.264 兼容资源参与分辨率、码率和体积选择；没有兼容流时明确报错，不再生成无效视频。

### 验证

- 137 项隔离基线自动化测试、编译检查和 Git 差异检查均通过。
- 使用真实小红书分享链接完成默认设置下载，输出为 720×1280、H.264 `avc1` + AAC MP4；便携 FFmpeg 全片解码无错误。
- 发布包：`VideoDownloadKing-v0.8.2-Windows-x64.zip`
- ZIP 体积：`162.14 MiB`
- SHA256：`D31029DA17ADE86AD6E7A9A45E63BC59AD1143C3BF51FF2149744C90C6216145`

## 0.8.1 - 2026-08-28

### 修复

- 兼容小红书页面初始状态中新出现的 `new Map([])` JavaScript 值，修复“笔记数据格式已变化，暂时无法解析”。
- 小红书视频流提取改为遍历页面返回的动态流分组，兼容 EF4、EF5 等新字段，不再只识别固定的 H.264/H.265 分组。
- 初始状态转换会区分字符串与 JavaScript 值，标题或正文中的普通 `new Map([])` 文本不会被误改。

### 验证

- 137 项自动化测试、编译检查和 Git 差异检查均通过。
- 使用真实小红书分享链接完成分析和下载，识别 4 条视频流及最高 1080×1920 分辨率；下载 MP4 经便携 FFprobe 验证包含视频流和 AAC 音频流。
- 发布包：`VideoDownloadKing-v0.8.1-Windows-x64.zip`
- ZIP 体积：`162.14 MiB`
- SHA256：`CA38693AA035F6962E40FE5BF1C2355707109D14CAC361D8C7122F3B7FFF52E7`

## 0.8.0 - 2026-07-27

### 新增

- 单链接下载页新增 Instagram 单个 Reel、视频帖子和旧式 IGTV 链接支持，复用现有 `yt-dlp` 分析、格式、封面、命名、平台分类、Cookie 和转码流程。
- Instagram 轮播或多视频帖子会在分析阶段明确拒绝；主页、Story、直播和批量内容仍不支持。
- 单链接下载页新增 TikTok 单视频长链及 `vm.tiktok.com`、`vt.tiktok.com`、`tiktok.com/t/...` 分享短链支持，复用现有 `yt-dlp` 下载链路。
- TikTok 分享短链解析后会再次校验作品类型；图片、轮播、主页、合集、音乐、标签和直播会被明确拒绝。

### 调整与修复

- 通用登录设置更名为“YouTube / Instagram / TikTok / X 登录”，Instagram 和 TikTok 继续复用现有浏览器 Cookie 或 Netscape `cookies.txt`，不新增配置字段。
- TikTok 链接在未配置代理时与其他海外平台一样显示潜在网络问题提示。
- 四个下载页面的“打开保存目录”会在任务成功后打开实际输出文件所在目录；尚无成功任务或原目录已删除时回退到配置的保存根目录。

### 验证

- 137 项自动化测试、编译检查、Git 差异检查和打包 EXE 启动烟测均通过。
- 使用真实 TikTok 长链和 `vt.tiktok.com` 分享短链完成分析；实际下载 MP4 经 FFprobe 验证包含 HEVC 视频流和 AAC 音频流。
- 发布包：`VideoDownloadKing-v0.8.0-Windows-x64.zip`
- ZIP 体积：`158.18 MiB`
- SHA256：`7557C5356A3FA664DB856923D787357DFC3FA3742E6369BBD5BB0ED59C44F7F5`

## 0.7.0 - 2026-07-14

### 新增

- 新增独立“小红书下载”页，以自研网页解析内核支持单篇视频、图文、图集、实况图片、短链和分享文本，不调用 `yt-dlp`，不执行转码。
- 小红书内核支持初始状态解析、原始视频与 H.264/H.265 选流、图片格式转换 CDN、实况 MP4、备用地址、Range 续传和原子落盘。
- 图文笔记自动保存到独立目录；增加平台/作者分类、命名模板、视频偏好、图片格式、封面和独立 Netscape `cookies.txt` 设置。

### 调整与修复

- 四个下载页的链接输入框统一增加“清除”和“粘贴”按钮；下载成功后不再弹窗，完整输出路径改为写入页面日志。
- 抖音图集实际成功保存两个及以上文件时，自动原子提交到独立作品目录；单文件仍保持原有输出方式，重复目录自动编号。
- 抖音、B站和小红书未设置专用 Cookie 时回退到 YouTube / X 通用 `cookies.txt`；四项 Cookie 文件设置均可一键清除。
- 主窗口任务关闭检查、滚动页面适配和设置迁移纳入小红书页面。
- 第三方说明记录 XHS-Downloader 的公开链路研究来源；小红书实现未复制或捆绑其 GPL-3.0 源码。

### 验证

- 97 项自动化测试、编译检查、Git 差异检查和打包 EXE 启动烟测均通过。
- 使用真实小红书视频笔记完成带 Cookie 的分享文本分析、最高分辨率视频及封面下载；FFprobe 验证输出为有效 HEVC + AAC MP4。
- 发布包：`VideoDownloadKing-v0.7.0-Windows-x64.zip`
- ZIP 体积：`158.18 MiB`
- SHA256：`D6F28A76F30DF54154BE0AEB6390C5C2765530EAE3C8AFFF4CAD3FE08D16D609`

## 0.6.0 - 2026-07-13

### 新增

- 单链接下载页新增 X（`x.com` / `twitter.com`）单条视频帖子及 `/video/序号` 链接分析下载，复用 yt-dlp 格式、封面、字幕、命名和转码流程。
- 新增独立“B站下载”页，以自研 WBI + DASH 内核支持 BV、AV、`b23.tv`、分享文本和多分P，不调用 `yt-dlp`。
- B站内核支持画质/编码/音质选择、4路 Range 分片、备用 CDN、无转码合并，以及封面、SRT 字幕、ASS 弹幕和 NFO 元数据。
- YouTube、抖音和 B站页面的命名变量按钮改为更宽松的间距、内边距和自动换行布局。

### 调整与修复

- B站页面增加独立 Cookie 设置，并兼容旧网站 Cookie；修复 Windows 下 UTF-8 Cookie 文件读取失败。
- B站下载控制区增加“打开保存目录”按钮。
- 修复带方括号的命名模板在重复文件检测中被当作 glob 模式的问题。
- X URL 校验支持帖子页面复制出的 `/video/序号` 后缀。

### 验证

- 自动化测试、编译检查、Git 差异检查以及 B站/X 真实分析下载均通过。
- 发布包：`VideoDownloadKing-v0.6.0-Windows-x64.zip`
- ZIP 体积：`158.13 MiB`
- SHA256：`09D0E13C49B477AE8A54D6DC4A9DCFBEF0A68263C66802806A407B9209882781`

## 0.5.3 - 2026-06-19

### 新增

- 单链接下载页加入 Shutter Encoder 风格的视频编码面板，支持图像比例、竖构图、旋转、镜像和禁止放大。
- 视频码率支持 VBR、CBR、CQ，以及 `auto`、好的、最好档位、最大限度、目标文件大小、二次编码和最高质量。
- 音频转换支持 AAC、MP3、AC3、声道与采样率设置。
- 硬件加速拆分为解码、滤镜和编码能力检测，并保留 GPU 失败询问回退 CPU。
- YouTube 与抖音分析增加独立“停止分析”按钮。

### 调整与修复

- 抖音下载页移除视频编码模块和额外转码流程。
- 修复“仅封面”设置导致输出模式和视频编码控件被永久锁定的问题。
- 整体界面改为更紧凑的按钮、输入框、复选框、间距和表格行高。
- 恢复下拉框原生展开箭头。
- 单链接页改为三段式工作区：顶部信息区、中部独立滚动的输出/编码区、可拖动高度且具有最低高度的底部状态区。
- 旧版转码配置自动迁移到新的码率和硬件字段。

### 验证

- 单元测试、Qt UI 测试、编译检查和 Git 差异检查全部通过。
- 发布包：`VideoDownloadKing-v0.5.3-Windows-x64.zip`
- ZIP 体积：`158.01 MiB`
- SHA256：`505DD44CDF588757D7E17BD94B9B7682C265EDD6962311EEC9C6F4CFD9CCCB77`

## 0.5.2 - 2026-06-18

### 新增

- YouTube 和抖音分析任务增加忙碌进度提示。
- 分析期间启用取消按钮，可终止因网络问题卡住的分析子进程。
- YouTube、Instagram、X 链接在未配置代理时提示中国大陆网络环境下可能无法连接。
- 代理设置页增加异步网络连通性测试，可自定义测试网址，默认使用 Google 官网。
- 连通性测试支持直连、HTTP、HTTPS、SOCKS4 和 SOCKS5，并直接使用尚未保存的当前表单配置。

### 改进

- 分析取消被识别为正常取消状态，不再弹出“分析失败”对话框。
- 连通性测试运行时保持设置窗口响应，并显示成功状态、HTTP 状态码和耗时。

### 验证

- 单元测试、Qt UI 测试、编译检查和 Git 差异检查全部通过。
- 发布包：`VideoDownloadKing-v0.5.2-Windows-x64.zip`
- ZIP 体积：`157.99 MiB`
- SHA256：`0C44EB1ED0031B52EB04C0C4004CB07BA2E152F6F73DB235678F18A76C36E143`

## 0.5.1 - 2026-06-16

### 改进

- 主窗口默认尺寸调整为更适合 1080p 的 `1180x760`，最小尺寸调整为 `1024x680`。
- 单链接页、抖音页和批量页统一放入可伸缩滚动区域，窗口高度不足时可通过垂直滚动访问底部控件。
- 降低格式表格最小高度，避免表格把窗口最小高度撑爆。
- 固定 PyInstaller `VideoDownloadKing.spec`，发布脚本改为通过 spec 构建，避免生成式命令漂移。
- 移除 `--collect-all gmssl`，改为只保留抖音签名需要的 `gmssl.func` 和 `gmssl.sm3`。
- 发布包裁剪未使用的 Qt translations、QtQuick、QtQml、QtPdf、OpenGL 软件渲染 DLL 和非必要 platform 插件。

### 验证

- 新增 UI 尺寸和打包配置测试。
- `v0.5.1` ZIP 体积约 `157.99 MB`，相比 `v0.5.0` 的 `176.44 MB` 减少约 `18.45 MB`。
- 发布包：`VideoDownloadKing-v0.5.1-Windows-x64.zip`
- SHA256：`BAED9D946EBEB9AAEC50CFBFD48E12B843083DBE39353F785A7AC13C2653D044`

## 0.5.0 - 2026-06-16

### 新增

- 新增独立“抖音下载”页面，支持单视频、图集、短链接和分享文本。
- 抖音视频支持自研引擎与 `yt-dlp` 双引擎，失败时由用户确认是否切换。
- 自研引擎支持 A-Bogus/X-Bogus、本地随机 msToken、多接口重试和无水印资源优选。
- 支持图集原图、实况片段、顺序命名、部分完成警告和原子文件提交。
- 新增独立抖音 Netscape `cookies.txt` 配置。
- 抖音视频复用现有 FFprobe/FFmpeg 兼容 MP4 与 GPU 失败回退流程。

### 调整

- 平台识别扩展至抖音单作品，同时保留 YouTube 现有行为。
- 版本提升至 `0.5.0`，发布包增加异步网络和签名依赖。

## 0.4.0 - 2026-06-10

对应提交：`85c78ff`

### 新增

- 接入 V1 正式品牌 Logo，用于 Windows EXE、任务栏和软件窗口左上角图标。
- 增加“总任务”和“当前阶段”双进度条。
- 下载进度显示当前格式流、速度、总大小和 ETA。
- 分析后可打开字幕选择窗口。
- 人工字幕和自动字幕分组显示，支持搜索和多选。
- 自动字幕默认只显示中文、英文等常用语言，可选择显示全部。
- 字幕输出支持 SRT 和 VTT。
- 增加 `SubtitleInfo`、`SubtitleSelection` 和结构化进度字段。

### 修复与调整

- 显式启用 yt-dlp `--progress`，修复使用 `--print` 后下载进度不显示。
- 聚合视频流和音频流进度，第二条流开始时总进度不再倒退。
- 合并、封面、字幕、检测和转码纳入统一任务阶段。
- 人工字幕与自动字幕分开调用 yt-dlp，避免来源混淆。
- 修复文件名包含方括号时无法正确回查字幕和封面的问题。
- 字幕下载失败只记录警告，不影响主视频任务成功。
- 保留旧字幕配置的加载兼容，但不再按旧语言表达式自动选择字幕。

## 0.3.0 - 2026-06-10

对应提交：`f58f4fe`

### 新增

- 输出模式增加“仅封面”。
- 增加编码感知的 H.264 自动码率决策。
- VP8、VP9、HEVC、AV1、MPEG-4 和 MPEG-2 使用不同码率补偿系数。
- 未知源编码自动使用受约束质量模式和 `maxrate`/`bufsize` 体积保护。
- 自动质量决策改为结构化结果，日志可显示源编码、源码率、倍率和目标码率。
- 增加 UI 测试，覆盖数字框按钮和仅封面模式联动。

### 界面调整

- 所有数字输入框移除右侧上下按钮，下拉框仍保留展开箭头。
- “命名与附属”从“兼容 MP4”中移出，改为独立全宽“下载选项”区域。
- 仅封面模式下禁用画质、格式流、字幕和兼容 MP4 控件。

### 移除与兼容

- 移除“将封面嵌入视频”控件、配置和 FFmpeg 逻辑。
- `DownloadArtifacts.media_path` 改为可空，允许任务只产生封面。
- 旧配置中的 `embed_thumbnail` 字段会被安全忽略。

## 0.2.0 - 2026-06-10

对应提交：`5c5a533`

### 新增

- 输出模式明确为“视频+音频”“仅视频”“仅音频”“高级流组合”。
- 增加仅视频下载。
- 增加自定义命名模板：
  - `{title}`
  - `{id}`
  - `{channel}`
  - `{platform}`
  - `{upload_date}`
  - `{download_date}`
- 增加下载封面、下载字幕和封面嵌入功能。
- 增加转码文件后缀：自动编码后缀、自定义后缀或无后缀。
- 增加 CPU/GPU 选择和 NVIDIA、Intel、AMD 厂商选择。
- 不支持的硬件编码器在 UI 中禁用。
- GPU 转码失败时询问是否回退 CPU。
- 增加视频和音频流选择摘要。

### 修复与优化

- 所有 yt-dlp、FFmpeg、FFprobe、Deno 和 `taskkill` 子进程隐藏 CMD 窗口。
- NVENC 探测画面改为 `640x360`，修复部分显卡误判。
- 自动码率优先读取视频流码率、容器码率、文件大小估算和分析提示。
- 转码、重封装和部分流转码后按实际输出编码追加后缀。
- 视频流表格高度增加，至少显示约 10 行。
- 表格选中行和复选框样式增强。
- 旧配置的 `video` 输出模式迁移为 `video_audio`。

## 0.1.0 - 2026-06-10

对应提交：`04e0b6e`

### 初始版本

- 建立 Python 3.12 + PySide6 模块化桌面项目。
- 支持 YouTube 单链接分析和下载。
- 支持最高、2160p、1440p、1080p、720p、480p、最低及自定义画质。
- 支持视频流与音频流高级组合。
- 支持视频、纯音频及原始音频、AAC、M4A、MP3 输出。
- 支持 HTTP、HTTPS、SOCKS4、SOCKS5 代理。
- 支持 `cookies.txt` 和 Chrome/Edge Cookie。
- 支持按 YouTube 平台子目录分类保存。
- 使用 FFprobe 检测媒体容器及编码。
- 使用 FFmpeg 重封装或转为 H.264 + AAC MP4。
- 实际试编码检测 NVIDIA NVENC、Intel QSV 和 AMD AMF，不可用时使用 CPU。
- 支持实时日志、取消任务和便携配置。
- 配置损坏时自动备份并恢复默认值。
- 提供批量下载占位页面。
- 提供 PyInstaller 便携 ZIP 构建脚本。
- 随附 yt-dlp，并提供 FFmpeg、FFprobe 和 Deno 下载脚本。
